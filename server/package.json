{
  "name": "halo-outlook-server",
  "version": "0.1.0",
  "description": "Express static server for the Halo Outlook add-in and API endpoints",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "js-yaml": "^4.1.0",
    "sql.js": "^1.14.2"
  }
}
